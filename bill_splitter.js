//Task 1- Calulate the tip

let billSize = 200; // Assuming a customer pays $200

// Using a ternary operator to calculate the tip
var tip = (billSize >= 50 && billSize <= 300) ? billSize * 0.15 : billSize * 0.2;

